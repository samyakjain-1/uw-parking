{
  "name": "uw-parking-map",
  "version": "1.0.0",
  "description": "A live parking map for UW Madison.",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
